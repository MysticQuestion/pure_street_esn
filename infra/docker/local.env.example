# Local environment variables for ESN development

# PostgreSQL/PostGIS connection
POSTGRES_DB=esn
POSTGRES_USER=esn
POSTGRES_PASSWORD=esn_dev_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Object storage (MinIO)
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=esnminio
MINIO_SECRET_KEY=esnminio_dev_password
MINIO_BUCKET=esn-media

# FastAPI server
ESN_API_HOST=0.0.0.0
ESN_API_PORT=8000

# Supabase (if used for auth/storage)
SUPABASE_URL=
SUPABASE_ANON_KEY=